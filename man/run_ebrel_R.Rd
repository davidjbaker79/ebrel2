% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_ebrel_R.R
\name{run_ebrel_R}
\alias{run_ebrel_R}
\title{Run an EBREL Optimisation from Terra Rasters}
\usage{
run_ebrel_R(
  E,
  C,
  SD,
  D,
  SxH,
  O,
  action_weight,
  LM,
  X0 = NULL,
  opt = list(),
  sentinel = 1e+10
)
}
\arguments{
\item{E}{A single-layer \link[terra:SpatRaster]{terra::SpatRaster} containing the existing habitat
class in each cell. Values must use the internal zero-indexed habitat
coding:
\describe{
\item{\code{-1}}{No existing habitat.}
\item{\code{0:(n_habitats - 1)}}{Existing habitat class.}
}}

\item{C}{A multilayer \link[terra:SpatRaster]{terra::SpatRaster} containing the cost of applying
each action in each cell. It must contain one layer per action. The first
\code{n_actions - 1} layers represent habitat creation actions and the final
layer represents habitat improvement. Costs approximately equal to
\code{sentinel} are treated as unavailable actions.}

\item{SD}{A multilayer \link[terra:SpatRaster]{terra::SpatRaster} containing one species
distribution layer per species. Raster geometry must match \code{C}.}

\item{D}{A numeric or integer vector containing one dispersal-distance value
per species. Its length must equal the number of layers in \code{SD}.}

\item{SxH}{A numeric matrix with species in rows and habitat creation classes
in columns. It must have dimensions \code{n_species} by \code{n_habitats}, where
\code{n_habitats = n_actions - 1}. Non-zero entries indicate that a habitat is
suitable for a species.}

\item{O}{A numeric vector containing one biodiversity target per species.
Its length must equal the number of layers in \code{SD}.}

\item{action_weight}{A numeric vector containing one contribution weight per
action. Its length must equal the number of layers in \code{C}. The final value
gives the contribution weight assigned to habitat improvement.}

\item{LM}{A single-layer \link[terra:SpatRaster]{terra::SpatRaster} containing the binary land mask.
Values must be \code{0} or \code{1}, and raster geometry must match \code{C}.}

\item{X0}{An optional single-layer \link[terra:SpatRaster]{terra::SpatRaster} containing the initial
action configuration. Raster geometry must match \code{C}. Values must use the
internal EBREL action coding:
\describe{
\item{\code{-1}}{No action selected.}
\item{\code{0:(n_actions - 1)}}{Zero-indexed selected action.}
}
When \code{NULL}, an initial solution is generated internally according to the
options supplied in \code{opt}. Defaults to \code{NULL}.}

\item{opt}{A named list of optimisation, calibration, temperature-tuning,
stopping-rule, random-seed, and output options passed to the C++ runner.
Unspecified options use their internal defaults.}

\item{sentinel}{Numeric scalar identifying unavailable cell-action
combinations in \code{C}. Defaults to \code{1e10}.}
}
\value{
A named list containing optimisation results and diagnostics returned
by the C++ runner. The \code{X_best} element is reconstructed as a single-layer
\link[terra:SpatRaster]{terra::SpatRaster} with the same geometry as \code{E}. Its values use the
internal EBREL action coding:
\describe{
\item{\code{-1}}{No action selected.}
\item{\code{0:(n_actions - 1)}}{Zero-indexed selected action.}
}

Other elements may include the initial solution, objective value,
species-target contributions, objective-component traces, acceptance
diagnostics, iteration counts, objective scaling, and temperature-tuning
diagnostics.
}
\description{
High-level user-facing wrapper for running an EBREL spatial optimisation
from raster-based model inputs.
}
\details{
The function validates and aligns the supplied rasters, extracts their values
into the flat vector layouts expected by the C++ implementation, runs the
optimisation, and reconstructs the best solution as a
\link[terra:SpatRaster]{terra::SpatRaster}.

An optional initial solution may be supplied through \code{X0}. When \code{X0} is
\code{NULL}, an initial solution is generated internally by the C++ optimiser.

Raster values are flattened in action-major or species-major order as
required by the C++ implementation. For \code{C}, all cells from the first action
layer are followed by all cells from the second action layer, and so on.
Species distribution layers in \code{SD} are flattened in the equivalent
species-major order.

\code{SxH} is transposed before conversion so that it is passed to C++ in
species-major, row-wise order.

When \code{X0} is \code{NULL}, the wrapper passes an empty integer vector to C++. This
signals that the initial action configuration should be generated internally.
A supplied \code{X0} is passed unchanged after validation; it must therefore
already use zero-indexed internal action codes.
}
\examples{
\dontrun{
result <- run_ebrel_R(
  E = ebrel_sim_data$E,
  C = ebrel_sim_data$C,
  SD = ebrel_sim_data$SD,
  D = ebrel_sim_data$D,
  SxH = ebrel_sim_data$SxH,
  O = ebrel_sim_data$O,
  action_weight = c(
    rep(1, terra::nlyr(ebrel_sim_data$C) - 1L),
    0.1
  ),
  LM = ebrel_sim_data$LM,
  X0 = NULL,
  opt = list(
    n_iterations = 10000,
    rng_seed = 123,
    verbose = TRUE
  )
)

terra::plot(result$X_best)

X0 <- hot_start_greedy(ebrel_sim_data)

hot_result <- run_ebrel_R(
  E = ebrel_sim_data$E,
  C = ebrel_sim_data$C,
  SD = ebrel_sim_data$SD,
  D = ebrel_sim_data$D,
  SxH = ebrel_sim_data$SxH,
  O = ebrel_sim_data$O,
  action_weight = c(
    rep(1, terra::nlyr(ebrel_sim_data$C) - 1L),
    0.1
  ),
  LM = ebrel_sim_data$LM,
  X0 = X0
)
}

}
\seealso{
\code{\link[=hot_start_greedy]{hot_start_greedy()}}
}
