% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vector_to_spatraster.R
\name{vector_to_spatraster}
\alias{vector_to_spatraster}
\title{Convert a Flat Vector to a SpatRaster Stack}
\usage{
vector_to_spatraster(vec, nh, dim1, dim2, extent = NULL, crs = NA)
}
\arguments{
\item{vec}{A numeric vector of length \code{dim1 * dim2 * nh}, where each segment of length \code{dim1 * dim2}
corresponds to one habitat layer, stored in column-major order.}

\item{nh}{Integer. Number of habitat layers (i.e., number of raster bands).}

\item{dim1}{Integer. Number of columns in the raster (x dimension).}

\item{dim2}{Integer. Number of rows in the raster (y dimension).}

\item{extent}{Optional. A \code{SpatExtent} object or extent values (passed to \code{terra::ext()}).
Defines the spatial extent of the output raster.}

\item{crs}{Optional. Coordinate reference system string or object (passed to \code{terra::crs()}).}
}
\value{
A \code{SpatRaster} object with \code{nh} layers and dimensions \verb{[dim2, dim1]}.
}
\description{
Converts a flattened vector (returned by C++ code in column-major order)
into a \code{SpatRaster} object with multiple layers using the \code{terra} package.
}
\examples{
# Example with 2 layers of 3x3 raster
vec <- c(1:9, 11:19)
r <- vector_to_spatraster(vec, nh = 2, dim1 = 3, dim2 = 3)
plot(r)

}
