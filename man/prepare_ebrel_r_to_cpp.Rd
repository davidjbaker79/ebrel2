% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_ebrel_r_to_cpp.R
\name{prepare_ebrel_r_to_cpp}
\alias{prepare_ebrel_r_to_cpp}
\title{Prepare EBREL Inputs for the C++ Backend}
\usage{
prepare_ebrel_r_to_cpp(
  E_rast,
  C_rast,
  SD_rast,
  D_vec,
  SxH_mat,
  O_vec,
  sigma,
  LM_rast,
  sentinel = 1e+10
)
}
\arguments{
\item{E_rast}{A single-layer \link[terra:SpatRaster]{terra::SpatRaster} containing the existing
habitat class in each landscape cell. Values must use the internal
zero-indexed habitat coding:
\describe{
\item{\code{-1}}{No existing habitat.}
\item{\code{0:(n_habitats - 1)}}{Existing habitat class.}
}}

\item{C_rast}{A multilayer \link[terra:SpatRaster]{terra::SpatRaster} containing one cost layer per
action. The first \code{n_actions - 1} layers represent habitat creation
actions and the final layer represents habitat improvement. Cell-action
combinations with costs equal or approximately equal to the configured
sentinel value are interpreted as unavailable by downstream C++ code.}

\item{SD_rast}{A multilayer \link[terra:SpatRaster]{terra::SpatRaster} containing one baseline
species-distribution layer per species. Its geometry must match \code{E_rast}
and \code{C_rast}.}

\item{D_vec}{A numeric vector containing one maximum dispersal-distance value
per species. Its length must equal the number of layers in \code{SD_rast}.}

\item{SxH_mat}{A numeric, integer, or logical matrix describing
species-habitat associations. Rows correspond to species and columns
correspond to habitat creation classes. It must have dimensions
\code{n_species} by \code{n_habitats}, where
\code{n_habitats = n_actions - 1}.}

\item{O_vec}{A numeric vector containing one biodiversity target per species.
Its length must equal the number of layers in \code{SD_rast}.}

\item{sigma}{A positive numeric scalar controlling the strength of distance
decay used when weighting habitat creation proposals. Proposal weights are
based on
\deqn{w = \exp(-\sigma d),}
where (d) is distance from the relevant species distribution.}

\item{LM_rast}{A single-layer \link[terra:SpatRaster]{terra::SpatRaster} containing the binary land
mask. Values must be \code{0} or \code{1}, and its geometry must match the other
raster inputs.}
}
\value{
A named list containing:
\describe{
\item{\code{dim_x}}{Number of raster columns.}
\item{\code{dim_y}}{Number of raster rows.}
\item{\code{n_actions}}{Number of action layers in \code{C_rast}.}
\item{\code{n_species}}{Number of species layers in \code{SD_rast}.}
\item{\code{n_habitats}}{Number of habitat creation classes, equal to
\code{n_actions - 1}.}
\item{\code{E}}{Integer vector of length \code{dim_x * dim_y}, containing the
categorical existing-habitat map.}
\item{\code{C}}{Numeric vector of length
\code{dim_x * dim_y * n_actions}, stored in action-major order.}
\item{\code{SD}}{Numeric vector of length
\code{dim_x * dim_y * n_species}, stored in species-major order.}
\item{\code{D}}{Numeric vector of length \code{n_species}.}
\item{\code{SxH}}{Numeric vector of length
\code{n_species * n_habitats}, stored in species-major row order.}
\item{\code{O}}{Numeric vector of length \code{n_species}.}
\item{\code{sigma}}{Validated distance-decay parameter.}
\item{\code{LM}}{Integer vector of length \code{dim_x * dim_y}.}
}
}
\description{
Converts raster, vector, and matrix inputs into the flattened data structures
used by the EBREL C++ backend.
}
\details{
The function validates the dimensions and basic coding of the supplied model
inputs, extracts raster values, and returns a named list containing flat
vectors and model dimensions.

Raster values from \code{C_rast} and \code{SD_rast} are extracted as matrices with
cells in rows and layers in columns. Calling \code{as.vector()} on these matrices
produces layer-major vectors: all cells from layer 1 are followed by all
cells from layer 2, and so forth. This matches the action-major and
species-major layouts used by the C++ backend.

\code{E_rast} and \code{LM_rast} are single-layer rasters and are therefore returned
as vectors of length \code{dim_x * dim_y}.

Because R matrices are stored column-major, \code{SxH_mat} must be transposed
before flattening when the C++ code expects species-major row order:
\code{as.numeric(t(SxH_mat))}.
}
\examples{
\dontrun{
obj <- prepare_ebrel_r_to_cpp(
  E_rast = ebrel_sim_data$E,
  C_rast = ebrel_sim_data$C,
  SD_rast = ebrel_sim_data$SD,
  D_vec = ebrel_sim_data$D,
  SxH_mat = ebrel_sim_data$SxH,
  O_vec = ebrel_sim_data$O,
  sigma = ebrel_sim_data$sigma,
  LM_rast = ebrel_sim_data$LM
)
}

}
